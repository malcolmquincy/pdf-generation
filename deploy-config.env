# PDF Service Deployment Configuration
# Edit these values before running the deployment script

# Service Configuration
SERVICE_NAME="pdf-service"
SERVICE_DIR="/opt/pdf-service"
SERVICE_PORT="3001"

# Domain Configuration (change to your domain or IP)
NGINX_DOMAIN="localhost"

# Git Repository (leave empty if copying files manually)
GIT_REPO="https://github.com/malcolmquincy/pdf-generation.git"

# Optional: Email for Let's Encrypt SSL (uncomment to enable HTTPS)
# SSL_EMAIL="your-email@domain.com"

# Optional: Additional security settings
ENABLE_RATE_LIMITING="true"
ENABLE_FIREWALL="true"